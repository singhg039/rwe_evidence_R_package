% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ehr_readers.R
\name{rwe_read_ehr}
\alias{rwe_read_ehr}
\title{Read EHR Data}
\usage{
rwe_read_ehr(
  source,
  format = c("omop_cdm", "fhir", "cerner", "epic", "custom"),
  version = NULL,
  lazy = FALSE,
  validate = TRUE,
  date_range = NULL,
  schema = NULL,
  ...
)
}
\arguments{
\item{source}{Character. Path to file, database connection, or API endpoint}

\item{format}{Character. Data format: "omop_cdm", "fhir", "cerner", "epic", "custom"}

\item{version}{Character. Format version (e.g., "5.4" for OMOP CDM)}

\item{lazy}{Logical. Use lazy evaluation for large datasets}

\item{validate}{Logical. Perform data validation on load}

\item{date_range}{Date vector. Optional date range filter c(start, end)}

\item{schema}{Character. Database schema name (for database connections)}

\item{...}{Additional arguments passed to reader functions}
}
\value{
An rwe_ehr object containing:
\item{data}{The loaded data (tibble or arrow dataset)}
\item{metadata}{Source information, load timestamp, validation results}
\item{schema}{Detected or specified schema information}
}
\description{
Reads electronic health record data from various sources with automatic
format detection and validation.
}
\examples{
\dontrun{
# Read OMOP CDM data from CSV
ehr <- rwe_read_ehr(
  source = "data/omop_cdm.csv",
  format = "omop_cdm",
  version = "5.4"
)

# Read from database
library(DBI)
con <- dbConnect(RPostgres::Postgres(),
                 dbname = "ehr_db",
                 host = "localhost")
ehr <- rwe_read_ehr(
  source = con,
  format = "omop_cdm",
  schema = "cdm_data"
)
}

}
