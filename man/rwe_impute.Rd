% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{rwe_impute}
\alias{rwe_impute}
\title{ML-Based Data Imputation}
\usage{
rwe_impute(
  data,
  method = c("mean", "median", "mode", "mice", "random_forest", "knn", "xgboost"),
  vars = NULL,
  predictors = NULL,
  m = 5,
  seed = NULL,
  k = 5,
  xgb_params = list(),
  xgb_nrounds = 50
)
}
\arguments{
\item{data}{Data with missing values (data frame or rwe_data object)}

\item{method}{Imputation method: "mean", "median", "mode", "mice",
"random_forest", "knn", "xgboost"}

\item{vars}{Variables to impute (NULL for all variables with missing data)}

\item{predictors}{Variables to use as predictors (NULL for all complete variables)}

\item{m}{Number of imputed datasets for multiple imputation (MICE only)}

\item{seed}{Random seed for reproducibility}

\item{k}{Number of neighbors for kNN imputation}

\item{xgb_params}{Named list of XGBoost parameters}

\item{xgb_nrounds}{Number of boosting rounds for XGBoost imputation}
}
\value{
rwe_imputation object with imputed data and diagnostics
}
\description{
Imputes missing values using machine learning algorithms with
proper validation and uncertainty quantification
}
\examples{
\dontrun{
# Simple mean imputation
imputed <- rwe_impute(
  data = patient_data,
  method = "mean"
)

# Random forest imputation
imputed_rf <- rwe_impute(
  data = patient_data,
  method = "random_forest",
  vars = c("lab_value", "blood_pressure"),
  seed = 123
)

# Multiple imputation
imputed_mice <- rwe_impute(
  data = patient_data,
  method = "mice",
  m = 5,
  seed = 123
)
}

}
