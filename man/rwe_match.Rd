% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity_score.R
\name{rwe_match}
\alias{rwe_match}
\title{Match Cohorts Using Propensity Scores}
\usage{
rwe_match(
  ps_object,
  method = c("nearest", "optimal", "caliper"),
  ratio = 1,
  caliper = 0.1,
  replace = FALSE
)
}
\arguments{
\item{ps_object}{rwe_propensity_score object from rwe_propensity_score()}

\item{method}{Matching method: "nearest", "optimal", "caliper"}

\item{ratio}{Matching ratio (e.g., 1 for 1:1, 2 for 1:2)}

\item{caliper}{Maximum propensity score difference (on logit scale)}

\item{replace}{Allow replacement in matching}
}
\value{
rwe_matched_cohort object with matched data and balance statistics
}
\description{
Performs matching between treatment and control groups using propensity scores
}
\examples{
\dontrun{
# First estimate propensity scores
ps <- rwe_propensity_score(
  data = study_data,
  treatment = "treated",
  covariates = c("age", "sex")
)

# Then perform matching
matched <- rwe_match(
  ps_object = ps,
  method = "nearest",
  ratio = 1,
  caliper = 0.1
)

print(matched)
}

}
